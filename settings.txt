1. folder generate

mkdir CVA
cd CVA
code .

├─data/
│  ├─raw/
│  ├─processed/
│  └─external/
├─notebooks/
├─src/
│  ├─data/
│  ├─models/
│  ├─training/
│  ├─inference/
│  └─utils/
├─experiments/
├─outputs/
│  ├─figures/
│  ├─checkpoints/
│  └─logs/
├─configs/
├─tests/
│
├─requirements.txt
├─.gitignore
└─README.md

2. Virtual Environment

py -m venv .venv
    Ctrl + Shift + P
    Python: Select Interpreter
    .venv

3. Package Install (in .venv)

pip install numpy pandas matplotlib scikit-learn
pip install jupyter ipykernel
pip install pyyaml tqdm
requirements.txt
    numpy
    pandas
    matplotlib
    scikit-learn
    torch
    torchvision
    opencv-learn
    pydicom
    nibabel
    tqdm
Colab
    !pip install -r requirements.txt

4. Github Settings

(Local)
git init
git add .
git commit -m "Initial setup"

git remote add origin https://github.com/colab-HS/CVA
git branch -M main
git push -u origin main

5. Colab Settings

(cell 1)
import torch

print(torch.__version__)
print(torch.cuda.is_available())

!nvidia-smi

(cell 2)
from google.colab import drive
drive.mount('/content/drive)

(cell 3)
!git clone https://github.com/colab-HS/CVA
%cd CVA
!pip install -r requirements.txt